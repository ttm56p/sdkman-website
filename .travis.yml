script: ./gradlew -q -s generateSite

jdk:
  - openjdk7

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
  local_dir: site/build/site
  repo: github.com:sdkman/beta.sdkman.github.io.git

